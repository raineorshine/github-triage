# A GitHub token with access to your notifications.
#
# Either a classic PAT with the `repo` scope (which covers notification reads
# and writes), or reuse the gh CLI's token:
#
#   echo "GITHUB_TOKEN=$(gh auth token)" > .env.local
#
# Read server-side only — it is never sent to the browser.
GITHUB_TOKEN=
